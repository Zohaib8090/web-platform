export type Video = {
  id: string;
  title: string;
  description: string;
  category: string;
  thumbnailUrl: string;
  videoUrl: string;
  imageHint: string;
  duration: string;
};

export type Category = {
  id: string;
  name: string;
};
